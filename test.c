/*#include 	"test.h"
int main() {
    int num, i;

	char[] slorgef = "dfgergwef;
    char sg3df = 'srgw';
    for (i = 1; i <= num; ++i) {
        if (num % i == 0) {
            printf("%d ", i);
        }
    }
    return 0;*/

    /*
        printf("Enter a positive integer: ");
        scanf("%d", &num);

    */
        //printf("Factors of %d are: ", num);
            char[] 	awraf = "sdsdf";

            int g = 1.5e3;

    	int d = 0X123f;
        int sdasdqw = 0x123A2;
        bool sdfs = true;
        bool sdqwe = false;
    	char sdf = 'f';
    	struct Books {
           char  title[50];
           char  author[50];
           char  subject[100];
           int   book_id;
        };
        struct Books Book1;
        struct Books Book2;

        Book1.book_id = 34.234;
}
